// What a block's notes page looks like while its route loads.

import { BlockPageSkeleton } from "@/components/app/notes/skeletons";

export default function Loading() {
  return <BlockPageSkeleton />;
}
