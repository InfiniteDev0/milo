// The handwritten face for the mockup only; the app's own fonts are untouched.

import { Patrick_Hand } from "next/font/google";

export const hand = Patrick_Hand({ subsets: ["latin"], weight: "400" });
